# Core dependencies
flask>=3.0.0
openai>=1.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
weasyprint>=60.0
mem0ai>=0.1.0

# Vector database
qdrant-client>=1.7.0

# Search provider (DuckDuckGo - free, no API key required)
duckduckgo-search>=4.0.0

# Optional search providers (uncomment to use):
# tavily-python>=0.3.0       # Tavily (good for research, has free tier)
# google-search-results>=2.4.2  # SerpAPI (most reliable, paid)
